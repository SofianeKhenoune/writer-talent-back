{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(form) }}
    <button class="btn btn-sm btn-success mt-3 mb-3 bi bi-check-lg">{{ button_label|default(' Enregistrer') }}</button>
{{ form_end(form) }}
