{% extends 'base.html.twig' %}

{% block title %}Modifier un Genre{% endblock %}

{% block body %}
    <h1>Modifier un genre</h1>

    {{ include('genre/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a class="btn btn-sm btn-primary mt-3 mb-3" href="{{ path('app_genre_index') }}">Retour à la liste</a>

    {{ include('genre/_delete_form.html.twig') }}
{% endblock %}
