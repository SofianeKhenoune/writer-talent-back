{% extends 'base.html.twig' %}

{% block title %}Genre{% endblock %}

{% block body %}
    <h1>Genre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ genre.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-sm btn-primary mt-3 mb-3" href="{{ path('app_genre_index') }}">Retour à la liste</a>

    <a class="btn btn-sm btn-primary mt-3 mb-3" href="{{ path('app_genre_edit', {'id': genre.id}) }}">Modifier</a>

    {{ include('genre/_delete_form.html.twig') }}
{% endblock %}
