{% extends 'base.html.twig' %}

{% block title %}Genre{% endblock %}

{% block body %}
    <h1>{{ genre.name }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ genre.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-sm btn-primary mt-3 mb-3 bi bi-skip-backward" href="{{ path('app_genre_index') }}"> Retour</a>

    <a class="btn btn-sm btn-warning mt-3 mb-3 bi bi-pencil-square" href="{{ path('app_genre_edit', {'id': genre.id}) }}"> Modifier</a>

    {{ include('genre/_delete_form.html.twig') }}
{% endblock %}
